package homework.hw6;

public interface UserView<T extends User> {
    // void sendOnConsole(List<T> list); // Исходный код: метод принимает конкретную реализацию списка.
    // Принцип единственной ответственности: интерфейс определяет только один метод для отправки данных на консоль.
    // Принцип инверсии зависимостей: метод принимает абстракцию (Iterable), а не конкретную реализацию (List).
    void sendOnConsole(Iterable<T> iterable); // Мой вариант: метод принимает абстракцию для повышения гибкости.
}